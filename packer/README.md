# doge-tasks-service-packer
Creates images for running doge-tasks-service with Packer

## Instructions
* Set `AWS_ACCESS_KEY-ID` and `AWS_SECRET_ACCESS_KEY` environment variables.
* `packer build packer.json`